### ***NOTE: REMOVE IN-LINE COMMENTS TO ENSURE DOTENV FILE COMPATIBILITY*** ###

### Required name as a base for Terraform resources and logs ###
AGENT_NAME="your-agent-name"


### Required GitHub repository information for Terraform CI/CD setup ###
GITHUB_REPO_NAME="your-repo-name"
GITHUB_REPO_OWNER="your-github-username-or-organization"


### Google Cloud configuration ###
### Required
GOOGLE_GENAI_USE_VERTEXAI="true"
GOOGLE_CLOUD_PROJECT="your-project-id"
GOOGLE_CLOUD_LOCATION="us-central1"
GOOGLE_CLOUD_STORAGE_BUCKET="your-staging-bucket"

### Optional agent code subdirectory in the staging bucket
GCS_DIR_NAME="your-agent-engine-staging-directory"


### Optional Agent application runtime environment variables ###
### Logging verbosity (defaults to "INFO" if unset) - must be one of "DEBUG", "INFO", "WARNING", "ERROR", "CRITICAL"
LOG_LEVEL="DEBUG"

### OTel genai event content capture (defaults to "true" if unset) - https://opentelemetry.io/blog/2024/otel-generative-ai/#example-usage
OTEL_INSTRUMENTATION_GENAI_CAPTURE_MESSAGE_CONTENT="true"


### Optional Environment Variables for Agent Engine Deployment ###
### Display Name and Description default to "ADK Agent" if left unset
AGENT_DISPLAY_NAME="Your Agent Engine Display Name"
AGENT_DESCRIPTION="Your Agent Engine Description"

### Leave unset to deploy a new Agent Engine instance or set the value to a deployed instance ID to update it
### Example: for Agent Engine Resource name `projects/PROJECT_ID/locations/LOCATION/reasoningEngines/1234567890123456789` -> AGENT_ENGINE_ID="1234567890123456789"
# AGENT_ENGINE_ID


### Optional Environment Variables for Agentspace app registration ###
### Leave unset if not using Agentspace or register the Agent Engine instance with this Agentspace App ID
# AGENTSPACE_APP_ID

### Location only used when registering an Agent Engine instance with an Agentspace App - must be one of "global", "us", or "eu"
# AGENTSPACE_APP_LOCATION="global"


### Optional OAuth credentials for BigQuery user authentication ###
### Required when using Gemini Enterprise (Agentspace) with user OAuth
# OAUTH_CLIENT_ID="your-oauth-client-id"
# OAUTH_CLIENT_SECRET="your-oauth-client-secret"

### Gemini Enterprise auth ID - the key in tool_context.state for the access token
### This is set by Agentspace when the user authenticates
# GEMINI_ENTERPRISE_AUTH_ID="bigquery_token"


### Other optional features ###
# ADK_SUPPRESS_EXPERIMENTAL_FEATURE_WARNINGS="true"

### Model override (default: gemini-2.5-flash)
# ROOT_AGENT_MODEL

### Local development server port (default: 8000)
# PORT
